#include<stdio.h>
int main()
{
float length,breadth,area;
printf("enter the length of the rectangle:");
scanf("%f",&length);
printf("enter the breadth of the rectangle:");
scanf("%f",&breadth);
area=length*breadth;
printf("enter the area of rectangle is:%.2f\n",area);
scanf("%f",&area);
return 0;
}
